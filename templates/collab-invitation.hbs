<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nothub Invitation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }
        .header {
            background-color: #333;
            color: #ffffff;
            text-align: center;
            padding: 20px;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
        }
        .body {
            padding: 20px;
            color: #333;
        }
        .body p {
            font-size: 16px;
            line-height: 1.6;
        }
        .details {
            margin: 20px 0;
            background-color: #f4f4f4;
            padding: 15px;
            border-left: 5px solid #333;
        }
        .details h3 {
            margin-top: 0;
            font-size: 18px;
            color: #333;
        }
        .button {
            display: inline-block;
            margin: 10px 5px;
            padding: 10px 15px;
            font-size: 16px;
            text-decoration: none;
            color: #fff;
            border-radius: 5px;
        }
        .accept {
            background-color: #28a745;
        }
        .reject {
            background-color: #dc3545;
        }
        .footer {
            background-color: #333;
            color: #ffffff;
            text-align: center;
            padding: 15px;
            font-size: 14px;
        }
        .footer a {
            color: #ffffff;
            text-decoration: none;
            margin: 0 10px;
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <h1>Notespacehub</h1>
        </div>

        <!-- Body Section -->
        <div class="body">
            <p>Hello, {{toName}}</p>
            <p>You’ve been invited to collaborate on <strong>{{title}}</strong> by <strong>{{byName}}</strong>.</p>

            <div class="details">
                <h3>Invitation Details</h3>
                <p><strong>Title:</strong> {{title}}</p>
                <p><strong>Collaborate As:</strong> {{as}}</p>
            </div>

            <p>To join this collaboration, click on the button below to accept the invitation. If you're not interested, you can also reject the invitation.</p>

            <!-- Action Buttons -->
            <a href="{{acceptLink}}" class="button accept">Accept Invitation</a>
            <a href="{{rejectLink}}" class="button reject">Reject Invitation</a>
        </div>

        <!-- Footer Section -->
        <div class="footer">
            <p>&copy; 2024 Nothub. All Rights Reserved.</p>
            <a href="{{appLink}}">Home</a> |
            <a href="{{contactLink}}">Contact Us</a> |
            <a href="{{privacyLink}}">Privacy Policy</a>
        </div>
    </div>

</body>
</html>
